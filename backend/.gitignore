# 文件说明：backend 局部忽略规则。
# 职责：防止 Python 缓存、虚拟环境、本地数据库和真实 fixture 数据进入仓库。
# 更新触发：新增后端构建产物、运行时文件、真实样本数据或本地工具缓存时，需要同步更新本文件。

__pycache__/
*.py[cod]
.venv/
*.sqlite
*.sqlite3
.pytest_cache/
runtime/fixtures/**
!runtime/fixtures/**/
!runtime/fixtures/**/README.md
runtime/testdata/**

